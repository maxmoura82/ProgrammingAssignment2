#I didn't understand the exercise very well. I tried, but I have problems. I gave up. Sorry. 

#I saw the model on internet and I copy, but, I continued don'tundersantand. 



makeCacheMatrix <- function(x = matrix()) {

        inv <- NULL

        set <- function(y) {

                x <<- y

                inv <<- NULL

        }

        get <- function() x

        setInverse <- function(inverse) inv <<- inverse

        getInverse <- function() inv

        list(set = set,

             get = get,

             setInverse = setInverse,

             getInverse = getInverse)

}

 

cacheSolve <- function(x, ...) {

        ## Return a matrix that is the inverse of 'x'

        inv <- x$getInverse()

        if (!is.null(inv)) {

                message("getting cached data")

                return(inv)

        }

        mat <- x$get()

        inv <- solve(mat, ...)

        x$setInverse(inv)

        inv

}



#Using the function

my_matrix <- makeCacheMatrix(matrix(9:12, 2, 2))

my_matrix$getInverse()

cacheSolve(my_matrix)
